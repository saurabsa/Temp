{
    "tasks": [
        {
            "label": "build (VSCE)",
            "command": "vsce",
            "args": [
                "up",
                "--iterate"
            ],
            "options": {
                "cwd": "${workspaceRoot}"
            },
            "problemMatcher": "$msCompile"
        }
    ]
}